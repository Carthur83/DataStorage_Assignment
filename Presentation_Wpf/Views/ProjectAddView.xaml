<UserControl x:Class="Presentation_Wpf.Views.ProjectAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation_Wpf.Views"
             xmlns:viewmodel="clr-namespace:Presentation_Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:ProjectAddViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="600" d:DesignWidth="500">
    <Grid Margin="20">
        
        <StackPanel>
            <TextBlock Text="Project Name" FontSize="14"/>
            <TextBox Text="{Binding Form.ProjectName}" FontSize="14" Padding="7"/>

            <TextBlock Text="Start Date" FontSize="14"/>
            <TextBox Text="{Binding Form.StartDate}" FontSize="14" Padding="7"/>

            <TextBlock Text="End Date" FontSize="14"/>
            <TextBox Text="{Binding Form.EndDate}" FontSize="14" Padding="7"/>

            <TextBlock Text="Customer Name" FontSize="14"/>
            <TextBox Text="{Binding Form.CustomerName}" FontSize="14" Padding="7"/>

            <TextBlock Text="Status" FontSize="14"/>
            <TextBox Text="{Binding Form.CurrentStatus}" FontSize="14" Padding="7"/>

            <TextBlock Text="Employment Number" FontSize="14"/>
            <TextBox Text="{Binding Form.ProjectManager.Id}" FontSize="14" Padding="7"/>

            <TextBlock Text="PM First Name" FontSize="14"/>
            <TextBox Text="{Binding Form.ProjectManager.FirstName}" FontSize="14" Padding="7"/>

            <TextBlock Text="PM Last Name" FontSize="14"/>
            <TextBox Text="{Binding Form.ProjectManager.LastName}" FontSize="14" Padding="7"/>

            <TextBlock Text="Service Name" FontSize="14"/>
            <TextBox Text="{Binding Form.ServiceName}" FontSize="14" Padding="7"/>

            <Button Content="Add" Padding="7" Margin="15 30 15 0" Command="{Binding AddProjectCommand}"/>
        </StackPanel>

        
    </Grid>
</UserControl>
