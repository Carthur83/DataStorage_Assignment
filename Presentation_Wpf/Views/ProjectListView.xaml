<UserControl x:Class="Presentation_Wpf.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation_Wpf.Views"
             xmlns:viewmodel="clr-namespace:Presentation_Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:ProjectListViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="650" d:DesignWidth="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="0*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="All Projects" FontSize="20" FontWeight="Medium" HorizontalAlignment="Center" />
                
            </Grid>
        <Grid Grid.Row="1">
            <ListView ItemsSource="{Binding Projects}" Width="auto" Height="auto">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Namn" DisplayMemberBinding="{Binding ProjectName}" Width="200"/>
                        <GridViewColumn Header="Tidsperiod" DisplayMemberBinding="{Binding StartDate.Date}" Width="200"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding CurrentStatus}" Width="200"/>

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
            

    </Grid>
</UserControl>
